datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Invoice {
  id       String   @id @default(uuid())
  customer String
  amount   Int
  status   String
  dueDate  DateTime
}
